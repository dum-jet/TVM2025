AddMul {
  Exp
    = AddExp

  AddExp
    = AddExp "+" MulExp -- plus
    | AddExp "-" MulExp -- minus
    | MulExp

  MulExp
    = MulExp "*" Atom  -- times
    | Atom

  Atom
    = "(" Exp ")"  -- paren
    | number

  number  (a number)
    = digit+           

}